в первом случае выведется 
10 10 10 10 10
т.к в функцию передаётся указатель, он меняется только внутри, а снаружи функции продолжает указывать на начало строки

во втором случае выведеся
10 9 8 7 6
т.к. в функцию передаётся указатель на указатель, при изменении указателя по указателю он меняется и внутри с снаружи функции, и с каждым инкриментов начинает указывать на следующую букву в строке.
